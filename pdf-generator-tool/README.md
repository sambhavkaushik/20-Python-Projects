# 🧰 Basic Starter PDF Generator (Python + fpdf2)

A simple, lightweight command-line tool that turns plain text into a neat PDF — complete with title, author, headers, footers, and Unicode support.

---

## ⚙️ 1. Install

```bash
pip install fpdf2
